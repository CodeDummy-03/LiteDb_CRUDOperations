<Window x:Class="TeamCity_WPFApplicationCodeFlow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
            <Style TargetType = "Button">
                <Setter Property = "Height" Value = "20" />
                <Setter Property = "Width" Value = "100" />
                <Setter Property = "Margin" Value = "10" />
            </Style>
    </Window.Resources>
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Insertion of 1" x:Name="btn_insertion1" Click="btn_insertion1_Click"/>
            <Button Content="Insertion of 2" x:Name="btn_insertion2" Click="btn_insertion2_Click" />
            <Button Content="Updation of 1" x:Name="btn_updation1" Click="btn_updation1_Click" />
            <Button Content="Deletion of 3" x:Name="btn_deletion1" Click="btn_deletion1_Click" />
        </StackPanel>
        <DataGrid AutoGenerateColumns="True" x:Name="dg_sourceData"/>
    </StackPanel>
</Window>
